'use strict';

describe('gotowankoApp.search_view module', function() {

  beforeEach(module('gotowankoApp.search_view'));

  describe('search_view controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var searchCtrl = $controller('SearchCtrl');
      expect(searchCtrl).toBeDefined();
    }));

  });
});